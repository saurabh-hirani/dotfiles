#compdef show-aws-profile

_show-aws-profile() { 
    local curcontext="$curcontext" state line
    typeset -A opt_args

    _arguments '1: :->csi'

    case $state in
    csi)
      _arguments "1: :($(aws-profiles))"
    ;;
    esac
}

_show-aws-profile "$@"
